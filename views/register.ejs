<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration - Event Management</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #262526, #747575);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .registration-container {
            background: #fff;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
        }

        .registration-container h1 {
            margin: 0 0 1.5rem;
            font-size: 1.8rem;
            text-align: center;
            color: #333;
        }

        .registration-container label {
            font-size: 0.9rem;
            color: #555;
            display: block;
            margin-bottom: 0.5rem;
        }

        .registration-container input {
            width: 100%;
            padding: 0.8rem;
            margin-bottom: 1.2rem;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 1rem;
        }

        .registration-container button {
            width: 100%;
            padding: 0.8rem;
            border: none;
            border-radius: 5px;
            background-color: #2575fc;
            color: #fff;
            font-size: 1rem;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        .registration-container button:hover {
            background-color: #000;

        }

        .registration-container .login-link {
            text-align: center;
            margin-top: 1rem;
        }

        .registration-container .login-link a {
            color: #2575fc;
            text-decoration: none;
        }

        .registration-container .login-link a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="registration-container">
        <h1>Event Management Registration</h1>
        <form action="/register" method="POST">
            <label for="username">Full Name</label>
            <input type="text" id="username" name="username" placeholder="Enter your full name" required>

            <label for="email">Email Address</label>
            <input type="email" id="email" name="email" placeholder="Enter your email" required>

            <label for="password">Password</label>
            <input type="password" id="password" name="password" placeholder="Enter your password" required>

           

            <button type="submit" onclick="redirectToIndex()" >Register</button>

            <div class="login-link">
                <a href="/login">Already have an account? Login here</a>
            </div>
        </form>
    </div>
    <script>
        // JavaScript function to handle the redirection
        function redirectToIndex() {
            window.location.href = '/home'; // Redirect to the '/booking' route
        }
    </script>
</body>
</html>

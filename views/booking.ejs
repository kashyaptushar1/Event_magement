<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Event - Event Management</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #262526, #747575);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .book-event-container {
            background: #fff;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
        }

        .book-event-container h1 {
            margin: 0 0 1.5rem;
            font-size: 1.8rem;
            text-align: center;
            color: #333;
        }

        .book-event-container label {
            font-size: 0.9rem;
            color: #555;
            display: block;
            margin-bottom: 0.5rem;
        }

        .book-event-container input, .book-event-container select, .book-event-container textarea {
            width: 100%;
            padding: 0.8rem;
            margin-bottom: 1.2rem;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 1rem;
        }

        .book-event-container button {
            width: 100%;
            padding: 0.8rem;
            border: none;
            border-radius: 5px;
            background-color: #2575fc;
            color: #fff;
            font-size: 1rem;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        .book-event-container button:hover {
            background-color: #000;
        }

    </style>
</head>
<body>
    <div class="book-event-container">
        <h1>Book Your Event</h1>
        <form action="/book-event" method="POST">
            <label for="name">Full Name</label>
            <input type="text" id="name" name="name" placeholder="Enter your full name" required>

            <label for="phoneno">Phone Number</label>
            <input type="tel" id="phoneno" name="phoneno" placeholder="Enter your phone number" required>

            <label for="eventtype">Event Type</label>
            <select id="eventtype" name="eventtype"  required>
                <option value="">Select an event type</option>
                <option value="wedding">Wedding</option>
                <option value="party">Party</option>
                <option value="conference">Conference</option>
                <option value="other">Other</option>
            </select>

            <label for="message">Message</label>
            <textarea id="message" name="message" rows="4" placeholder="Enter additional details or special requests"></textarea>

            <button type="submit">Book Event</button>
        </form>
    </div>
</body>
</html>
